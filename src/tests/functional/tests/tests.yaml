charm_name: openstack-service-checks
gate_bundles:
  - base: focal
  - base: bionic
  - cinder: focal-yoga
smoke_bundles:
  - base: bionic
dev_bundles:
  - base: bionic
  - cinder: focal-yoga
target_deploy_status:
  nova-cloud-controller:
    workload-status: blocked
    workload-status-message: "Missing relations: image, compute, placement"
  ceph-radosgw:
    workload-status: blocked
    workload-status-message: "Missing relations: mon"
tests:
  - base:
      - tests.test_deploy.TestOpenStackServiceChecks
  - cinder:
      - tests.test_deploy.TestOpenStackServiceChecksCinder
      - tests.test_deploy.TestOpenStackServiceChecks
